services:
  - type: web
    name: sovereign-town
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB
        value: sovereign_town
      - key: NODE_VERSION
        value: "20"
